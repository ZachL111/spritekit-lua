# Golden Cases

The golden set keeps `turn pressure` and `collision risk` visible as named cases.

The main golden fixture is `fixtures/golden/scoreboard.csv`. The matrix fixture is `fixtures/golden/lane-matrix.csv`. Together they cover `turn pressure`, `map entropy`, `collision risk`, and `visibility` with different score ranges.

The purpose is to make intentional rule changes show up in review.
